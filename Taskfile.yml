version: '3'

# synckit ships the synckitd daemon (cmd/synckitd); `build` compiles every package.

tasks:
  build:
    desc: Compile every package
    cmds:
      - go build ./...

  test:
    desc: Run the test suite with the race detector
    cmds:
      - go test -race -count=1 ./...

  vet:
    desc: Vet the module
    cmds:
      - go vet ./...

  lint:
    desc: Lint with golangci-lint
    cmds:
      - golangci-lint run

  fmt:
    desc: Format with gofumpt + goimports (via golangci-lint)
    cmds:
      - golangci-lint fmt

  generate-check:
    desc: Verify generated wire identity
    cmds:
      - go run ./rpc/gen -check

  ci:
    desc: Everything CI runs
    cmds:
      - task: generate-check
      - task: vet
      - task: lint
      - task: test
      - task: build
